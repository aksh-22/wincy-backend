export enum Role {
  'Admin' = 'Admin',
  'Member++' = 'Member++',
  'Member+' = 'Member+',
  'Member' = 'Member',
  'Client' = 'Client',
}

export enum Hierarchy {
  'Member',
  'Member+',
  'Member++',
  'Admin'
}
